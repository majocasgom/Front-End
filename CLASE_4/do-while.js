let count = 0; // Se inicializa la variable count en 0
//Ejecuta primero el bloque asi sea falso y despues verifica l condificon 

do{
    console.log(count); // Imprime el valor actual de count
    count++; // Incrementa count en 1
} while (count<5); // Verifica la condición
//Por ejemplo al introducir una contrasena, se repite hasta colocar la correcta 